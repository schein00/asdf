inteiro n, z, i, r;

entrada(n);

inteiro sq, flag = 1;

i = 1;
sq = 2;
enquanto( sq < n ){ 
	i = i+1;

	r = n % i;
	
	se( r == z ){
		flag = 0;
		saida( n, e divisivel por, , i );
	}

	sq = i * i;
}

se( flag == z ){
	saida( n, nao e primo );
}

se( flag != z ){
	saida( n, eh primo );
}
